{% extends 'medewerker.html.twig' %}

{% block menu %}
    {% if is_granted('ROLE_ADMIN') %}
    <li><a href="{{ path('activiteitenoverzicht') }}">home</a></li>
    <li><a href="{{ path('soortactiviteiten_new') }}">soort activiteiten</a></li>
    <li><a href="{{ path('user_index') }}">gebruikers</a></li>
    <li><a href="{{ path('beheer') }}">beheer</a></li>
    {% else %}
        <li><a href="{{ path('activiteiten') }}">home</a></li>
        <li><a href="/{{ app.user.id }}/edit" {%  if app.request.get('_route') == '/{{ app.user.id }}/edit' %} {% endif %}>gegevens</a></li>
    {% endif %}

{% endblock %}
{% block content %}
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_index') }}">back to list</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
